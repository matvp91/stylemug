name: CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  tests:
    runs-on: ubuntu-latest
    name: Tests
    
    steps:
    - uses: actions/checkout@v2
    - uses: stefanoeb/jest-action@1.0.3
    
