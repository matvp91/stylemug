{
  "name": "stylemug",
  "version": "1.0.6",
  "main": "dist/runtime.js",
  "license": "MIT",
  "files": [
    "dist",
    "compiler.js"
  ],
  "scripts": {
    "clean": "rimraf dist && rimraf compiler.js",
    "build": "yarn run clean && webpack --mode production"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "stylemug-compiler": "^1.0.0"
  }
}
